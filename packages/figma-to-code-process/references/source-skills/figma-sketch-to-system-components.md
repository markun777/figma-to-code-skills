# figma-sketch-to-system-components

Working folder for a future skill that expands sketches or low-fidelity
drafts into components that fit an existing design system.

## Why it matters

This is useful when the design is incomplete and we need to fill gaps,
but it should follow existing system rules instead of inventing a new
visual language by default.
